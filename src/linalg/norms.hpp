#ifndef NORMS_HPP
#define NORMS_HPP

double vec1norm(double* vec, int size);
double vec2norm(double* vec, int size);
double vecInfNorm(double* vec, int size);
double mat1norm(double* mat, int cols, int rows);
double matInfNorm(double* mat, int cols, int rows);

#endif
